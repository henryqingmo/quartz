- If   P(X=n) use   P(n – 0.5 < X < n + 0.5)
- If   P(X > n) use   P(X > n + 0.5)
- If   P(X ≤ n) use    P(X < n + 0.5)
- If    P (X < n) use   P(X < n – 0.5)
- If    P(X ≥ n) use   P(X > n – 0.5)
#math #statistics 